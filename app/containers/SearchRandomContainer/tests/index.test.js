// import React from 'react';
// import { shallow } from 'enzyme';

// import { SearchRandomContainer } from '../index';

describe('<SearchRandomContainer />', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(true);
  });
});
