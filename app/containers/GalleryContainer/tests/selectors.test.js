// import { fromJS } from 'immutable';
// import { makeSelectGalleryContainerDomain } from '../selectors';

// const selector = makeSelectGalleryContainerDomain();

describe('makeSelectGalleryContainerDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(true);
  });
});
