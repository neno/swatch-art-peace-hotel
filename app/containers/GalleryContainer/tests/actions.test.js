
import {
} from '../actions';
import {
} from '../constants';

describe('GalleryContainer actions', () => {
  describe('Default Action', () => {
    it('has a type of DEFAULT_ACTION', () => {
      expect(true).toEqual(true);
    });
  });
});
