// import { fromJS } from 'immutable';
// import { makeSelectNewsContainerDomain } from '../selectors';

// const selector = makeSelectNewsContainerDomain();

describe('makeSelectNewsContainerDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(true);
  });
});
