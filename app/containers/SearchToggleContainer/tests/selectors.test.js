// import { fromJS } from 'immutable';
// import { makeSelectSearchToggleContainerDomain } from '../selectors';

// const selector = makeSelectSearchToggleContainerDomain();

describe('makeSelectSearchToggleContainerDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
