import styled from 'styled-components';

const Detail = styled.div`
  display: flex;
  flex-direction: row;
  flex-wrap: wrap;
  position: relative;
  height: 100%;
  flex: 1;
`;

export default Detail;
